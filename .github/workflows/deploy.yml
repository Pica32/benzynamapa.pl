# Deploy probíhá automaticky přes Vercel GitHub integraci při každém push.
# Tento workflow není potřeba — Vercel buildí sám.
# Ponechán prázdný aby nepůsobil chyby v Actions.
name: Deploy (Vercel auto)

on:
  workflow_dispatch:

jobs:
  info:
    runs-on: ubuntu-latest
    steps:
      - name: Info
        run: echo "Deploy probíhá automaticky přes Vercel GitHub integraci."
